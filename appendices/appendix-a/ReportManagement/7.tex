\subsubsection{\textbf{Use Case: Delete Current Report}}

\textbf{Primary Actor:} User

\textbf{Trigger:} The user wants to delete the currently displayed report.

\textbf{Preconditions:} The user is authenticated, and the system displays a form containing the report metadata and any associated expenses.

\textbf{Postconditions:} The system deletes the currently displayed report.

\vspace{0.5cm}

\textbf{Main Success Scenario:}

\begin{enumerate} [leftmargin=0.75cm, noitemsep]
\item The user initiates the deletion of the currently displayed report.
\item The system prompts the user to confirm the deletion.
\item The user confirms the deletion.
\item The system unassigns all expenses associated with the report, if applicable, and marks the report as deleted in the database.
\item The system restores the standard Reports page view with the updated list (without displaying the recently deleted report).
\end{enumerate}

\vspace{0.3cm}

\textbf{Extensions:}

\begin{itemize} [label={}, leftmargin=0.75cm, noitemsep]

    \item \textbf{3a. Deletion Confirmation Rejected:}
    \begin{itemize} [label={}, leftmargin=0.75cm, noitemsep]
        \item 3a1. The user declines or closes the confirmation dialog.
        \item 3a2. The system closes the confirmation dialog and preserves the current report without deleting it.
    \end{itemize}

    \item \textbf{4a. Report Deletion Error:}
    \begin{itemize} [label={}, leftmargin=0.75cm, noitemsep]
        \item 4a1. An error occurs during the report deletion due to a database connection error or an internal error.
        \item 4a2. The system notifies the user of the error and keeps them on the same page without deleting the report.
    \end{itemize}

\end{itemize}

\vspace{0.3cm}
